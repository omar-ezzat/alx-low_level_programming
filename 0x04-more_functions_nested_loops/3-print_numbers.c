#include"main.h"

/**
 * _isupper - check if c is upper
 *
 * @c: input alphabet
 *
 * Return: 1 if the uppercase. 0 if not
 */

void print_numbers(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
		_putchar('0'+i);
	_Putchar('\n');
}
