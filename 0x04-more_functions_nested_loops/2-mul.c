#include"main.h"
/**
 * mul - function multiplies two integers 
 *
 * @a: input first num
 * @b: input second num
 *
 * Return: 1 if the uppercase. 0 if not
 */
int mul(int a, int b)
{
	return (a*b);
}
