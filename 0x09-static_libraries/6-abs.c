#include"main.h"
/**
 * _abs - function turn the number to the absolute
 *
 *@n: is the input number
 *Return: the absolute value of input number
*/
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
